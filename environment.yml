name: cortex
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
    - fastapi>=0.110.0
    - uvicorn[standard]>=0.29.0
    - pydantic>=2.6.0
    - pytest>=8.0.0
    - black>=24.2.0
    - httpx>=0.27.0
    - pyyaml>=6.0.0
    - rich>=13.0.0
